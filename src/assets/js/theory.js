// variable
var personaje = new Personaje()
// objeto.Metodo
personaje.avanzar(10)

// Estructura de control condicional
if (puerta) {
    puerta.abrir();
} else {
    usuario.voler()
}

// Estructura de control condicional
if (puerta.abierta()) {
    personaje.avanzar(1)
} else {
    personaje.quejarse()
}

// Estrcutura de control de casos
switch (personaje.nombre) {
    case 'Miguelito':
        // 
        break;
    default:
        //
}

contar = 1
...
contar = 2
...
contar = 3
...


contar = 10

for (let i = 0; i < 100000000; i++) {
    contar = i;
    console.log(contar)
}

ejecutar();

while (!enviado) {
    enviado = intentarEnviar();
}

do {
    ejecutar();
} while (condicion)

// tipos de datos
var numero = 0;
var texto = "Hola mundo"
var texto = 'Hola mundo'
var booleano = true // true o false
var arreglo = [0, 'hola', true, 14]
var objeto = { clave: 'valor' }

var funcion = function () {
    // instruccion 1
    // instruccion 2
}

function funcion() {
    var 
}